const DATE = 'date';

const NUMBER = 'number';

const SHORT_TEXT = 'short-text';

const SELECTOR = 'selector'; // not used

const PERSON = 'person';

const COMMENT = 'comment'; // not used

const TIMELINE = 'timeline';

const EXTERNAL_LINK = 'external-link';

const PERCETAGE = '#percentage';

const DEPENDENCY = 'dependency';

const LABEL = 'label';

const FILE = 'file'; // not used

export {
  DATE,
  NUMBER,
  SHORT_TEXT,
  SELECTOR,
  PERSON,
  COMMENT,
  TIMELINE,
  EXTERNAL_LINK,
  PERCETAGE,
  DEPENDENCY,
  LABEL,
  FILE,
};
