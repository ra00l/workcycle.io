'use strict';

const sequelize = require('./db');
const db = require('./db/models');

function Timer() {
  this._timer = null;
  this.start = function() {
  };

  function doCheck() {
  }
}

module.exports = Timer;
